"""Routes package marker.

Each route module is mounted by `papercast.server.app.create_app`. Keep
modules small and focused — one concern per file (papers / artifacts /
review / files / voice / config / ws).
"""
